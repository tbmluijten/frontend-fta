import { Alexandria, Inter, Manrope } from 'next/font/google';

export const inter = Inter({
  variable: '--font-inter',
  subsets: ['latin'],
  display: 'swap',
});

export const manrope = Manrope({
  variable: '--font-manrope',
  subsets: ['latin'],
  display: 'swap',
});

export const alexandria = Alexandria({
  variable: '--font-alexandria-raw',
  subsets: ['latin'],
  display: 'swap',
});
